[build]
builder = "nixpacks"

[phases.build]
cmds = [
  "npm install",
  "npm run build"
]

[deploy]
startCommand = "cd backend && node server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120