{
  "name": "artfolio",
  "version": "1.0.0",
  "description": "ArtFolio - Galerie d'art avec IA",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}